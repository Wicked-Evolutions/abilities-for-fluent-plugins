name: Ability Gap / Feature Request
description: Request a new ability or report a missing capability
title: "[Gap]: "
labels: ["enhancement"]
body:
  - type: markdown
    attributes:
      value: |
        📄 **Product scope is canonical** — site-agnostic product for arbitrary single-site **and** multisite WordPress; our sites are an incidental test environment, never the fix scope; site-coupled behavior is a defect; multisite is first-class. See [`docs/PRODUCT-SCOPE.md`](../docs/PRODUCT-SCOPE.md).
  - type: textarea
    id: description
    attributes:
      label: What ability is missing?
      description: Describe what you need to do that is not currently possible
    validations:
      required: true
  - type: textarea
    id: proposed
    attributes:
      label: Proposed ability
      description: "Ability name, parameters, expected output"
      value: |
        **Name:** `module/ability-name`
        **Parameters:**
        **Returns:**
    validations:
      required: true
  - type: dropdown
    id: tier
    attributes:
      label: Tier
      options:
        - Free (READ)
        - Pro (WRITE/DELETE)
        - Undecided
    validations:
      required: true
  - type: dropdown
    id: priority
    attributes:
      label: Priority
      options:
        - Critical — blocks abilities-only workflow
        - High — needed for alpha
        - Medium — needed before public launch
        - Low — nice to have
    validations:
      required: true
  - type: textarea
    id: context
    attributes:
      label: Additional context
      description: Specs, related abilities, workarounds currently in use
